//Criar variavel
let weight;

//Imprime na tela o tipo da variavel inserida
console.log(typeof weight);

//Objeto com variaveis do tipo String, Number, Float, Boolean
const yan = {
    name : "Yan Robert",
    age : 22,
    weight : 52.2,
    isSubscribed : true
}

//Dois exemplos com a mesma resposta ao imprimir na tela
console.log(person.name +" de idade " + person.age + " pesa " + person.weight + " kg");
console.log(`${person.name} de idade ${person.age} pesa ${person.weight} kg`);

//Array e colocando objetos dento do Array
let students = [];
students[0] = yan;

const edu = {
    name : "Edu",
    age : 24,
    weight : 70.20,
    isSubscribed : true
}

students[1] = edu;

console.log(students);

