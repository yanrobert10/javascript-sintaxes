// Operador Condicional

let pao = true;
let queijo = false;

const niceBreakfast = pao && queijo ? "Café Top" : "Café Ruim";
// Condição ? se for true : se for false; ternario


console.log(niceBreakfast);

// maior de 18
const canDrive = age >= 18 ? "can drive" : "can't drive";
console.log(canDrive);